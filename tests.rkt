#lang racket

(require (submod "tests/kons-list.rkt" test))
(require (submod "tests/kons-list-length.rkt" test))
(require (submod "tests/range.rkt" test))
(require (submod "tests/vektor.rkt" test))
(require (submod "tests/defaults.rkt" test))
(require (submod "tests/fail.rkt" test))
